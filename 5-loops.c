#include <stdio.h>

int main(){
    int i = 1, n;
    /* printf("Enter the value of n\n");
    scanf("%d", &n);

    while (i<=n)
    {
        printf("Natural no. is %d\n", i);
        i = i + 1; //   Increment Oprator(++)
    }
    */

   for(int i = 5; i >= 0; i--){ //Decrement Operator (--)
    printf("%d\n", i);
   }
    
    return 0;
}