//Array is the collection of similar elements.

#include <stdio.h>

int main(){
    int marks[10]; //initialisation of array (Reserve space to store 10 integer)
     marks[0] = 45;
     marks[1] = 50;
     marks[2] = 20;
    return 0;
}